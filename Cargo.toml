[package]
name = "fetris"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4"
rand = "0.9"

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.3", features = ["wasm_js"] }

[dev-dependencies]
insta = "1"
