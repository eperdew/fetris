[package]
name = "fetris"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4"
bevy_pkv = { version = "0.15", default-features = false, features = ["sled"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
insta = "1"
